document.getElementById("login-form").addEventListener("submit", function (e) {
    e.preventDefault();

    // Perform authentication logic here (e.g., checking credentials)

    // For demonstration purposes, always display an alert
    alert("Login successful!");
});